# iclogin
covid

# install

git clone ...
cd pycli/covid
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install -e .
which covid

# usage
```
$ covid
...

$ icl filenosuffix
```
